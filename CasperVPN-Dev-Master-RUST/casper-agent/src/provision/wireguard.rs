use anyhow::Result;

pub async fn install() -> Result<()> {
    // Placeholder: Implement distro detection and installation via apt/yum/dnf/pacman etc.
    println!("[provision] Installing WireGuard (stub)");
    Ok(())
}
