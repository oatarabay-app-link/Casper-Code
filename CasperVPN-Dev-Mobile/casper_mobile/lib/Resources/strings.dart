class Strings {
  Strings._();
  static const String debugTitle = 'Debug Screen';
  static const String generateButton = 'Register VPN (silent)';
  static const String success = 'Success — profile requested';
  static const String failure = 'Failed — see logs';
}
